# Gemini 站点绑定与模型目录

2026-09-21 排查分组 11 的「测试香蕉」Key：本地账号 31、38、44 绑定
`gemini-3.1-flash-image-preview`，账号 32、37 绑定
`gemini-3-pro-image-preview`。无 `-preview` 的名称并未绑定。

原 `/v1beta/models` 选择一个上游账号并透传它的目录，空凡返回无后缀 Flash
及带后缀 Flash，导致客户端选到本地不支持的名称，也看不到飞羽等账号已绑定的
Pro。`/v1/models` 原来已返回本地两个正确名称。

修复后，如果 Gemini 分组内账号都有明确模型限制，原生列表直接汇总本地名称，
站点托管账号以绑定策略的 `local_model` 为准，普通账号使用映射左侧名称，
按分组白名单过滤和去重。模型详情也能读取本地绑定的名称。存在无限制或通配符
账号时保留上游目录兼容，并补充其他账号明确配置的名称。混合 Antigravity 调度
继续遵守原有启用条件。不会自动把 Flash 改成 Pro，或把未绑定的上游模型暴露为
可用模型。

账号编辑的白名单下拉将已有选择并入选项，支持搜索、勾选和移除目录以外的新模型。
此前 `-preview` 名称虽已保存且在顶部显示，下拉只列内置名称，造成没有勾选项的
错觉。此修复不修改账号保存的模型范围。

空凡原生图片流可能用 HTTP 200 返回 `[keepalive]` 进度及候选文本中的
`GPT Image 引擎暂不可用，请稍后重试`。对空凡站点的图片绑定，发送客户端首个
有效输出之前检查有限前缀：明确引擎不可用、429/5xx 错误信封或只有进度就结束
时交给现有调度器换账号，失败不进入成功图片计费。内容拒绝、参数错误和已开始的
真实输出保持原响应。检查不下载图片，不缓存整个图片流，成功响应字节保持原样。

验证包括整个分组的 Flash/Pro 列表、模型详情、白名单过滤、上游名称不泄漏，
以及进度后失败、内容拒绝、参数错误、正常图片、长图片流与读取中断。前端验证
已有自定义名称能在白名单下拉搜索并取消选中，以及账号编辑表单回归。

此次仅修改、测试、提交和推送；按用户要求，构建与预览更新由另一窗口完成。
